apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-storage
  namespace: docker-registry
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi
